
#help: hlp : help for loading functions
function hlp () {
    declare file=$(grep -m 1 -l "function $1" $FLISTDIR/* 2>/dev/null)
    sed -nr "/^function $1/,/^##/p" $file | grep '^#help: ' | cut -f2- -d: | column -s: -t
    
}

