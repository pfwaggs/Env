
function _ArgParser ()
{
    declare x dir DIR
    [[ -v COMP_LINE ]] && x=$2 || x=$1
    declare -a list
    list=($(compgen -f $x))
    [[ ${#list[@]} -gt 0 ]] || list=($(compgen -f ${CDPATH//://$x } "${DIRSTACK[@]/%/$x}" | _FixList -e -u))
    printf '%s\n' "${list[@]}"
}

