function load () {
    declare f
    case $1 in
	'') . ~/.envpurge
	    . ~/.bloader $ENVFILES/_${USER^^}
	    ;;
	/*) echo loading $1; . $1 ;;
	*) for f in $*; do
		echo loading $f
		. $ENVFILES/$f
	    done
	    ;;
    esac
}
