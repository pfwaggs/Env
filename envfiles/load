function load () {
    declare f
    case $1 in
	'') . ~/.envpurge
	    . ~/.bloader $ENVDIR/_LOAD01
	    ;;
	/*) echo loading $1; . $1 ;;
	*) for f in $*; do
		echo loading $f
		. $ENVDIR/$f
	    done
	    ;;
    esac
}
