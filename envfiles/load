
function load () {
    declare f
    case $1 in
	'') . ~/.envurge; . ~/.envsrc ;;
	/*) echo loading $1; . $1 ;;
	*) for f in $*; do
		echo loading $f
		. $ENVDIR/envfiles/$f
	    done
	    ;;
    esac
}
