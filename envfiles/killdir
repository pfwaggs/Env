
function killdir () { #AzA
    [[ -f .wpr ]] && echo write protect ring found && return 0
    pushd .. >/dev/null
    eval declare path="~1"
    read -p "remove <$path> ? "
    [[ $REPLY =~ ^[yY] ]] && eval rm -r ${path@Q}
    popd +1 >/dev/null
} #ZaZ
