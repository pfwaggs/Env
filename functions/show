

__show () {
    COMPREPLY=()
    COMPREPLY=("${COMPREPLY[@]}" $(compgen -v -X '!&*' $2))
    COMPREPLY=("${COMPREPLY[@]}" $(compgen -v -X '!&*' ${2^^}))
}

show () {
    set | perl -nlE "/^$1=/i && say"
}

complete -F __show show
