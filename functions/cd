
#TAG:use

_cd () {
    declare x list=$(compgen -d $2)
    [[ $list ]] || list=$(compgen -d ../$2)
    [[ $list ]] || list=$(compgen -d ~/$2)
    COMPREPLY=("$(echo -e $list)")
}

complete -o filenames -F '_cd' cd
