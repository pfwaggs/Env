[[ $# -gt 0 ]] || return 0
for file in $*; do
    dir=${file%/*}
    [[ -v NORMAL ]] || echo ${file##*/}
    for f in $(cat $file); do
	[[ -s $dir/$f ]] || continue
	[[ -v NORMAL ]] && echo -n . || echo $f
	. $dir/$f
    done
    echo ''
done
