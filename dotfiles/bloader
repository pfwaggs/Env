[[ $# -gt 0 ]] || return 0
for file in $*; do
    [[ -v NORMAL ]] || echo ${file##*/}
    dir=${file%/*}
    for line in $(sed -n 's/#.*//;p' $file); do
 	[[ -v NORMAL ]] && echo -n . || echo $dir/$line
 	. "$dir/$line"
    done
    echo
done
unset file line dir
