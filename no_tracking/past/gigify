function gigify () { #AzA
    declare u=BKMGT b=1024 v=$(echo ${@//[^0-9.+-]}) s=${v//[0-9.]}
    v=${v//[+-]}
    [[ $@ =~ [bB] ]] || b=1000
    declare m=$(dc -e "$v Z 1 - 3 / p")
    v=$(dc -e "4 k $v $f $m ^ / p")
    printf "%s%7.3f%s\n" "${s: }" $v ${u:$m:1}
} #ZaZ
