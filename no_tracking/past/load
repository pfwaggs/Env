
function load () { #AzA
    declare f
    case $1 in
	'') . ~/.envpurge
	    . ~/.bloader $ENVDIR/envsrc{_pre,}
	    ;;
	/*) echo loading $1; . $1 ;;
	*) for f in $*; do
		echo loading $f
		. $ENVDIR/$f
	    done
	    ;;
    esac
} #ZaZ
