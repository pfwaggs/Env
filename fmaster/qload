
qload () {
    [[ $# ]] || return 0
    declare dir file item
    for file in "$@"; do
	if [[ $file =~ / ]]; then
	    source "$file"
	else
	    for dir in ${ENV_DIRS//:/ }; do
		[[ ! -s "$dir/$file" ]] && continue
		read -p "load $dir/$file ?" ans
		[[ $ans == y* ]] && source "$dir/$file" || continue
	    done
	fi
    done
}


